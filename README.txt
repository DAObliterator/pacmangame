Problems With Algo
when the iterator reaches a dead end the Current Position has already been modified , so to resume the functions in the call stack
that has been paused,the current position would be the one just before it entered the junction
